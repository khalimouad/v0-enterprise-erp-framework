# Ollama Configuration
# Default Ollama API endpoint (Ollama runs on localhost:11434 by default)
OLLAMA_API_URL=http://localhost:11434/api/generate

# Ollama model to use (ensure it's installed via: ollama pull llama2)
OLLAMA_MODEL=llama2

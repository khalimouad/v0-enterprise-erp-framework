import { ConfigSubPage } from "@/components/contacts/config-sub-page"

export default function StatusesPage() {
  return (
    <ConfigSubPage
      title="Statuts"
      description="Gérez la liste des statuts disponibles pour les contacts"
      configKey="statuses"
    />
  )
}

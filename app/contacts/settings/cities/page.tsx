import { ConfigSubPage } from "@/components/contacts/config-sub-page"

export default function CitiesPage() {
  return (
    <ConfigSubPage
      title="Villes"
      description="Gérez la liste des villes disponibles pour les contacts"
      configKey="cities"
    />
  )
}
